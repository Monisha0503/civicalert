name: Build and deploy Node.js app to Azure Web App - civicalert-frontend

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'

      - name: Install and build frontend
        working-directory: frontend
        run: |
          npm install
          npm run build

      - name: Zip frontend build
        run: cd frontend && zip -r ../frontend-release.zip dist/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-app
          path: frontend-release.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: frontend-app

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_D356C95E4FC54C8C964B896E571DB9F4 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_A27AB5AECA6C4ACD91CA02E86ECA02C4 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_2ED42205FA3C47998C13025D4952022A }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'civicalert-frontend'
          slot-name: 'Production'
          package: frontend-release.zip
